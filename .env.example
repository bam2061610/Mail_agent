# Minimal environment for setup-first deployments.
# All mailbox, AI, workflow, and scheduler settings are configured in the UI wizard.

SECRET_KEY=replace-with-random-32-byte-hex
DATABASE_URL=sqlite:///./data/mail_agent.db
PORT=8000
